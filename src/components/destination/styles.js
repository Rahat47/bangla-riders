import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    map: {
        marginTop: "20px",
        width: "100%",
        height: "80vh",
        padding: "10px",
        borderRadius: "5px",
        backgroundColor: "white"
    },
})